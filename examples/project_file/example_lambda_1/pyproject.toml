[project]
name = "example_lambda_1"
version = "0.1.0"
description = "Add your description here"
requires-python = "==3.13.*"
dependencies = [
    "aws-lambda-powertools[tracer]>=3.23.0",
    "pydantic"
]